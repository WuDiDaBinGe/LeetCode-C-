#include<bits/stdc++.h>
using namespace std;
// 二维dp
int main() {
    int T;cin>>T;
    for(int t = 0; t < T; t++) {
        int n;cin>>n;
        vector<int> nums(n + 1, 0);
        for(int i = 1; i <= n; i++) {
            cin>>nums[i];
        }
        vector<int> dp(n + 1, 0);


    }
    return 0;
}